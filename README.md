# Lokad.OoxPdf

Dependency-free OOXML-to-PDF renderer for `.pptx` and `.docx` files.

This repository is being implemented from `EXECPLAN.md`. The current code is an early skeleton and does not yet render documents.
